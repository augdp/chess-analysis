# uses: stockfish engine wrapper

class CPEvaluator:
    def __init__(self, engine_handle, depth=18, multipv=1):
    def evaluate(self, board):
        # returns { "eval_cp": x, "eval_mate": y }
    def _call_engine(self, board):
